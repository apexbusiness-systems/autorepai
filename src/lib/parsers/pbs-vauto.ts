export const parsePBS = (data: any) => {
  // Mock PBS parser
  console.log('Parsing PBS data');
  return { success: true, data };
};

export const parseVAuto = (data: any) => {
  // Mock vAuto parser
  console.log('Parsing vAuto data');
  return { success: true, data };
};
